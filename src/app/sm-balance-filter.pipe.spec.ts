import { SmBalanceFilterPipe } from './sm-balance-filter.pipe';

describe('SmBalanceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SmBalanceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
